apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: stable
controlPlaneEndpoint: "{{ cfg.clusterConfiguration.controlPlaneEndpoint }}"
networking:
  podSubnet: "{{ cfg.pod_network_cidr }}"
apiServer:
  certSANs:
  - "{{ cfg.apiserver_address }}"
  - "{{ cfg.clusterConfiguration.controlPlaneEndpoint.split(':')[0] }}"
  - "127.0.0.1"
