apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: "{{ metallb.l2AdvertisementName }}"
  namespace: "{{ metallb.namespace }}"
spec:
  ipAddressPools:
  - "{{ metallb.ipAddressPoolName }}"